[project]
name = "pytests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "refnd",
    "pytest>=9.0.3",
    "rdkit>=2026.3.1",
]

[tool.uv.sources]
refnd = { path = "../py" }

[dependency-groups]
dev = [
    "matplotlib>=3.11.0",
    "scipy>=1.17.1",
]
